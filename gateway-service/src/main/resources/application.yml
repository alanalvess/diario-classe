server:
  port: ${PORT}

jwt:
  secret: ${SECRET}
  expiration: ${JWT_EXPIRATION}

spring:
  config:
    import: optional:configserver:${CONFIG_URL}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL}
